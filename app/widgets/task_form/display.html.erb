<%= widget_div do %>
  <h2>New Task</h2>
  
  <%= form_for :task, :url => url_for_event(:submit), :remote => true do |f| %>
    <%= f.text_field 'name' %>
   
    <%= submit_tag "New task!" %>
  
  <% end %> 
  
<% end %>


<div id="hello" />
